{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../FullStackOpen/part2/phonebook && npm run build && @powershell Copy-Item dist -Recurse ../../../FullStackOpenPart3",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no text specified\" && exit 1"
  },
  "author": "Luke Hussey",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.7",
    "nodemon": "^3.1.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "morgan": "^1.10.0"
  }
}
